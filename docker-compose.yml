version: '3.8'

services:
  web:
    image: ${DOCKER_IMAGE:-studyai_local}
    build: .
    container_name: studyai_web
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
